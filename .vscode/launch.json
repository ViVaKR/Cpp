{
    "configurations": [
        {
            "name": "clang++ active file build",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": ["100"],
            "stopOnEntry": true,
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "clang++ active file build"
        }
    ],
    "version": "0.2.0"
}
